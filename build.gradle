allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'idea'

    idea {
        module {
            downloadSources = true
            downloadJavadoc = true
        }
    }
}
